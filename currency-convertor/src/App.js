import React from 'react'

const App = () => {
  // Hooks (method )  which is returning a array having => count--var ,setcount which is returning a value storing in array having var + func
  // we can made our own 
  // const [count , setcount]=useState(0);
  return (
    <>
    <div>
      {/* <h1 className='bg-black text-2xl text-sky-400'>Tabban</h1> */}
    </div>
    </>
    
  )
}

export default App
