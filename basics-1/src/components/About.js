import React from 'react'

const About = () => {
  return (
    <div>
      
    </div>
  )
}
//=>HOOKS && REDUX && CRUD (Create Read Update Delete :) )
//DOM => USE TO CHANGE HTML STRUCTURE  
//static && virtual => static=> write by default and virtually setNumber and usestate

//hooks=> 
//1=>USE ON STATES (TOGGLE METHOD || ON AND OFF)=>mutable (states are itself)
//=>props => destructurized 
//2=>Use Effects => (AYSNC AND AWAIT WORK )
//4useContext => USE TO SHARE DATA BETWEEN //3=> Useref 
// COMPONENTS
//5=>usecallback
//6=>usememo
//7=>usereducer
export default About;
